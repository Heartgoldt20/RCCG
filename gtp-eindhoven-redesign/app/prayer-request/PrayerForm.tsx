"use client";
import {FormEvent,useState} from "react";
const churchEmail="gtpeindhoven@rccgeurope.org";
export default function PrayerForm(){const[name,setName]=useState(""),[request,setRequest]=useState(""),[opened,setOpened]=useState(false);function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const subject=name.trim()?`Prayer request from ${name.trim()}`:"Prayer request";const body=`Name: ${name.trim()||"Prefer not to say"}\n\nPrayer request:\n${request.trim()}`;window.location.href=`mailto:${churchEmail}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;setOpened(true)}return <form className="prayerForm" onSubmit={submit}><label>Your name <span>(optional)</span><input value={name} onChange={event=>setName(event.target.value)} autoComplete="name" placeholder="Your name"/></label><label>Prayer request<textarea value={request} onChange={event=>setRequest(event.target.value)} rows={7} placeholder="Tell us how we can pray..." required/></label><div className="prayerPrivacy"><span aria-hidden="true">✦</span><p>Your message is prepared for the church prayer team in your own email app. Nothing is stored on this website.</p></div><button className="btn dark" type="submit">Prepare prayer request</button>{opened&&<p className="formStatus" role="status">Your email app should now be open. Review your message and press send when you are ready.</p>}</form>}
